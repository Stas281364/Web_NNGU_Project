// topSlider
const topSlider = new Swiper('.slider', {
    loop: true,
    spaceBetween: 20,
  
    // If we need pagination
    pagination: {
      el: '.slider__nav',
      clickable: true
    }
});